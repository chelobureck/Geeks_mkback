# Django settings
SECRET_KEY=django-insecure-$(openssl rand -base64 32)
DEBUG=False
ALLOWED_HOSTS=geeks-mkback-n4t9.vercel.app,localhost,127.0.0.1

# Database settings
# Для Vercel лучше использовать внешнюю БД (PostgreSQL), 
# но для Docker/локально оставим SQLite по умолчанию.
DB_NAME=db.sqlite3
# DATABASE_URL=postgres://user:password@host:port/dbname

# Email settings (настройте свои данные)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
DEFAULT_FROM_EMAIL=your-email@gmail.com
